function findChar(cadena, caracter)
{
    let exists = false;
    for(item of candena)
    {
        exist = (item == caracter)
        if (exists)
            break;
    }
    return exists;
}